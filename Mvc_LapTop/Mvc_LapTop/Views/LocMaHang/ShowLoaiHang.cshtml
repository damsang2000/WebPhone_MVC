@using Mvc_LapTop.Models;
@model List<LoaiHang>

<ul class="list-group list-group-flush text-left" style="width:280px">
    @{
        foreach (var item in Model)
        {
        <li class="list-group-item">
            <a href="@Url.Action("SanPhamTheoloai", "LocMaHang", new { @cd = item.MaLoaiHang })" style="text-transform:uppercase;text-decoration:none;">@item.TenLoaiHang</a>
        </li>
        }
        @*<li class="list-group-item"><a href="#" style="text-decoration: none; text-transform: uppercase"></a></li>*@
    }
</ul>
